<Page
    x:Class="NewFranceVacanceUWP.View.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewFranceVacanceUWP.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:NewFranceVacanceUWP.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewModel:UserViewModel/>
    </Page.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Username:"/>
                <TextBox Name="UserNameBox"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Password:"/>
                <StackPanel Orientation="Horizontal">
                    <PasswordBox Name="PasswordBox"/>
                    <Button Name="LoginButton" Content="Login" Command="{Binding LoginCommand}"/>
                </StackPanel>
            </StackPanel>
            <TextBlock Name="MessageBox" Text="{Binding Message, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</Page>
